// my-service.model.ts
export interface MyService {
  id: number; // Optional for new services
  title: string; // The title of the service
  title_class: string; // Optional class for styling
  description: string; // Optional description of the service
}
